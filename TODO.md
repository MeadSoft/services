# TODO

1. finish setting up the iam package 2. write a spec for the iam package. the spec must include a ERD diagram, an account creation flow with a diagram 3. write unit tests
2. finish setting up the login page for the haru cafe website
    1. add unit tests for the client iam services
    2. add end to end testing using playwright
3. create a GCP terraform dev setup for the restaurant-catalog-api system. the terraform setup should be placed in systems/restaurant-catalog-api/dev
    1. GCP cloud sql postgres server
        1. secret manager secrets for
    2. cloud storage bucket for file storage services

4. create a gcp terraform setup for storing

## TODO Laters

These are tasks that can be addressed later. Do not implement them yet

1. write a spec for expected package structure
    1. include a diagram showing the expected package structure
2. setup a health check standard
    1. use grafana or an open source alternative
3. setup a user interface tracking system
    1. contentsquare or an open source alternative
4. setup a cli client for this repositories unique commands
    1. serving its mkdocs wiki server
