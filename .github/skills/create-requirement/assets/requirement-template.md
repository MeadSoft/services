# OpenFastTrace Requirement Template

```markdown
# <Human-readable title>

`<artifact-type>~<unique-id>~<revision>`

<Requirement statement using RFC 2119 language>

Rationale:

<Optional. Why this requirement exists and what risk/problem it addresses>

Comment:

<Optional. Clarifications, constraints, or notes>

Needs: <optional artifact types, comma-separated>

Depends: <optional>

<optional requirement IDs listed with a dash in front of each and a new line for each ID>

Covers:

<optional requirement IDs listed with a dash in front of each and a new line for each ID>
```

## Minimal Valid Item

```markdown
## <Human-readable title>

`req~example-id~1`

The system MUST provide the specified behavior.
```
